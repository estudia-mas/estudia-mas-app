# Deja vacío para usar el proxy de Vite (/api → Django) y cookies same-origin.
# VITE_API_BASE_URL_LOCAL=

# VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000

# Producción: vacío = same-origin; o hostname de la API.
# VITE_API_BASE_URL_PROD=
